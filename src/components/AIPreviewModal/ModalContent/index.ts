/**
 * ModalContent Component Export
 * 
 * This file exports the ModalContent component to maintain a clean import structure.
 * It allows importing from the directory rather than the specific file.
 */
export { ModalContent } from './ModalContent';
