/**
 * GuideLines Component Export
 * 
 * This file exports the GuideLines component to maintain a clean import structure.
 * It allows importing from the directory rather than the specific file.
 */
export { GuideLines } from './GuideLines';
