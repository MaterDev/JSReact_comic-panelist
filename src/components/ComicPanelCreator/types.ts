import { Panel } from '../../../shared/types/panelTypes';
import { ExportFormat } from './Controls/Controls';

// Add any ComicPanelCreator-specific types here
export interface ComicPanelCreatorProps {
  // Add props as needed
}

// Re-export types that might be needed
export type { Panel, ExportFormat };
